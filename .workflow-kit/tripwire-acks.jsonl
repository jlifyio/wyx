{"slug":"no-rederive-tier-from-severity","reason":"lacks_grep_target","note":"Rule: do not re-derive an agent's model tier from 'does it assign severity'. It records a reasoning error (severity is table-fixed in drift-detection.md, so the task reads mechanical while the real judgment is category selection). No textual artifact distinguishes a tier chosen for the right reason from one chosen for the wrong reason — only the resulting pin is visible, and that IS enforced by the dispatch-pin check. Enforced by review; rationale in CLAUDE.md.","author":"junpei.nagata@jlify.io","ts":"2026-08-08T16:48:34Z","scope_ref":"v0.25.0"}
